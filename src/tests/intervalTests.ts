import { Interval } from "@num";
import { Print } from "@util";

// const sum = Interval.add([2, 4], [3, 5]);
// Print(sum);

// const diff = Interval.sub([2, 4], [3, 5]);
// Print(diff);

// const prod = Interval.mul([2, 4], [3, 5]);
// Print(prod);

const quot = Interval.div([2, 4], [3, 5]);
Print(quot);
